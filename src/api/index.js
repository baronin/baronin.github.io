import { createFakeUsers, createUser, getUsers, removeUserById, updateUser } from "@/api/users.js";

export const api =  {
  getUsers,
  createFakeUsers,
  removeUserById,
  updateUser,
  createUser
}
